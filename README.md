# Тестовое задание: Image Storage API

## Основные требования

- Реализовать **авторизацию пользователя**
- Создать **закрытый API endpoint** для загрузки изображений
    - Допустимые форматы: **PNG, JPEG**
    - Остальные форматы должны быть запрещены
- Создать **закрытый API endpoint** для получения списка изображений,
  загруженных текущим пользователем
- Создать **закрытый API endpoint** для получения конкретного изображения
- Создать **закрытый API endpoint** для удаления изображения

---

## Вводные условия

- Пользователь может:
    - просматривать **только свои изображения**
    - удалять **только свои изображения**
- Максимальный размер загружаемого файла: **5 МБ**

---

## Дополнительные требования (будет плюсом ⭐)

- Система должна корректно работать при нагрузке:
    - **100 000+ загрузок изображений в день**
- Дисковое пространство ограничено:
    - изображения необходимо **сжимать без значимой потери качества**
    - допускается **изменение формата изображения** (например, WebP)
- Одно и то же изображение может быть загружено много раз:
    - необходимо **избежать дублирования файлов**
    - одинаковые изображения не должны храниться несколько раз

---

## Ожидаемый результат

- API с авторизацией
- Защищённые маршруты
- Корректная валидация файлов
- Продуманная работа с хранилищем изображений
- Архитектурные решения, учитывающие масштабируемость и оптимизацию ресурсов

---

## 🚀 Запуск проекта

### Требования
- PHP 8.2+
- Composer
- Postgres (Local or Docker)
- Redis (для очередей и кеша)
- S3 Bucket (MinIO/AWS)

### 1. Установка зависимостей
```bash
composer install
```

### 2. Настройка окружения
Скопируйте `.env.example` в `.env` и настройте подключение к БД, Redis и S3.
```bash
cp .env.example .env
php artisan key:generate
```

**Важные переменные:**
```dotenv
QUEUE_CONNECTION=redis
FILESYSTEM_DISK=s3

AWS_ACCESS_KEY_ID=...
AWS_SECRET_ACCESS_KEY=...
AWS_DEFAULT_REGION=...
AWS_BUCKET=...
```

### 3. Миграции
```bash
php artisan migrate
```

### 4. Запуск очередей (Критично для обработки изображений!)
Обработка изображений (WebP конвертация) происходит в фоне. Без этого воркера картинки останутся в статусе `PENDING`.
```bash
php artisan queue:work
```


### Docker Setup (Рекомендуемый) 🐳

Проект полностью Docker-ready. Вам не нужно устанавливать PHP, Postgres или Redis локально.

1. **Копирование env файла**
```bash
cp .env.example .env
```

2. **Запуск контейнеров**
```bash
docker-compose up -d --build
```
Это запустит:
- `app`: PHP-FPM приложение
- `web`: Nginx веб-сервер (порт 9191)
- `queue`: Воркер очередей (для обработки картинок)
- `pgsql`: PostgreSQL
- `redis`: Redis
- `minio`: S3-совместимое хранилище (Console: port 8900, API: port 9000)

3. **Установка зависимостей и ключа**
Внутри контейнера:
```bash
docker-compose exec app composer install
docker-compose exec app php artisan key:generate
docker-compose exec app php artisan migrate
```

4. **Настройка MinIO (S3)**
Чтобы S3 работал локально через MinIO, убедитесь, что в `.env` прописано:
```dotenv
FILESYSTEM_DISK=s3
AWS_ACCESS_KEY_ID=sail
AWS_SECRET_ACCESS_KEY=password
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=images
AWS_ENDPOINT=http://minio:9000
AWS_USE_PATH_STYLE_ENDPOINT=true
```
*Примечание: Бакет `images` должен быть создан вручную через консоль MinIO (http://localhost:8900) или CLI.*

---

## 🛠 Особенности архитектуры

### Оптимизация и Scalability
1.  **WebP Конвертация**: Все изображения асинхронно конвертируются в WebP для экономии места.
2.  **S3 Zero-Copy Deduplication**: Если два пользователя загружают одинаковый файл, он **не загружается повторно**. Система проверяет хеш файла на S3 и переиспользует существующий blob.
3.  **Race Condition Safety**: Внедрена защита от удаления используемых файлов при конкурентных запросах.
4.  **Smart Cleanup**: Удаление изображения пользователя удаляет физический файл с S3 только тогда, когда на него больше никто не ссылается.
